## Packages
framer-motion | Smooth entrance animations and page transitions
react-icons | Additional icons if Lucide is insufficient (though Lucide is primary)
zod | Schema validation
@hookform/resolvers | For Zod integration with React Hook Form

## Notes
The API endpoints are defined in shared/routes.ts.
The resume download button will link to a placeholder or the provided static asset.
Images for projects will use Unsplash placeholders if not provided.
Theme will be professionally styled with a focus on typography and spacing.
